// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model ActivityCategory {
  id         String     @id @default(cuid())
  name       String     @unique
  custom     Boolean    @default(true)
  activities Activity[]
  createdAt  DateTime   @default(now())
}

model Activity {
  id          String           @id @default(cuid())
  title       String
  description String?
  client      String?
  status      String           @default("Pendente")
  categoryId  String
  category    ActivityCategory @relation(fields: [categoryId], references: [id]) // ← corrigido: era "Category", agora é "ActivityCategory"
  logs        ActivityLog[]    // ← adicionado
  createdAt   DateTime         @default(now())
  updatedAt   DateTime         @updatedAt
}

model ActivityLog {
  id         String   @id @default(cuid())
  activityId String
  activity   Activity @relation(fields: [activityId], references: [id], onDelete: Cascade)
  note       String
  createdAt  DateTime @default(now())
}

model FinanceCategory {
  id      String         @id @default(cuid())
  name    String         @unique
  kind    String         @default("A pagar")
  partner Boolean        @default(false)
  custom  Boolean        @default(true)
  entries FinanceEntry[]
}

model FinanceEntry {
  id                String          @id @default(cuid())
  title             String
  amount            Float
  dueDate           DateTime
  kind              String          @default("A pagar")
  category          FinanceCategory @relation(fields: [categoryId], references: [id])
  categoryId        String
  partner           String          @default("")
  notes             String          @default("")
  recurring         Boolean         @default(false)
  confirmed         Boolean         @default(false)   // ← novo campo
  paid              Boolean         @default(false)
  paidAt            DateTime?
  generatedFromId   String?
  generatedFrom     FinanceEntry?   @relation("FinanceEntryRecurrence", fields: [generatedFromId], references: [id], onDelete: SetNull)
  generatedChildren FinanceEntry[]  @relation("FinanceEntryRecurrence")
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt
}